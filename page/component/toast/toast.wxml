<!--以下为toast显示的内容-->
<view class="toast_content_box" wx:if="{{isShow}}">
  <view class="toast_content">
      <text>{{content}}</text>
  </view>
</view>
<view class="toast_screen" wx:if="{{isShow}}"></view>